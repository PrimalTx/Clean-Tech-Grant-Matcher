[build]
  command = "npm run build"
  publish = ".next"

[functions]
  node_bundler = "esbuild"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "22"

# Environment variables (set these in Netlify dashboard)
# REWIRING_AMERICA_KEY = "your_api_key_here"
